# Risk tolerance threshold in INR for auto-resolving matches
RISK_TOLERANCE_INR=500.00

# Allowed CORS origins for local development (specific localhost ports)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000

# Wildcard example (NOT FOR PRODUCTION - use specific frontend domain in production deployments)
# CORS_ORIGINS=*
